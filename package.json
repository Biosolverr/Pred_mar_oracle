{
  "name": "genlayer-prediction-market",
  "version": "0.1.0",
  "private": true,
  "description": "GenLayer prediction market dApp — Bradbury testnet",
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory frontend",
    "deploy": "vercel deploy --prod"
  }
}
